# Doko CLI 配置

# Kimi 配置
kimi:
  # API Key：直接填写或使用环境变量 ${KIMI_API_KEY}
  api-key: sk-6s2yFxmlBbZfRavIfG03iaIj7DkpOjWd2LN13JPFOZoqQXQW
  model: kimi-k2.5

# 日志配置
logging:
  level:
    com.dokocli: INFO
    root: WARN
  pattern:
    console: "%d{HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# Spring 配置
spring:
  main:
    web-application-type: none
    banner-mode: off
